server:
  port: ${PRODUCT_PORT:8080}

spring:
  application:
    name: productservice
  datasource:
    url: ${DB_URL:jdbc:postgresql://aws-0-eu-west-1.pooler.supabase.com:6543/postgres?prepareThreshold=0}
    username: ${DB_USER:postgres.uwqjtdwytjcnbjhndsfl}
    password: ${DB_PASSWORD:ADA-postgres}
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: ${DDL_AUTO:update}
    show-sql: false
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect

jwt:
  secret: ${JWT_SECRET:ADA-jwt-secret-key-for-signing-tokens}

gemini:
  api-key: ${GEMINI_API_KEY:}
