jwt:
  secret: ${JWT_SECRET:ADA-jwt-secret-key-for-signing-tokens}

gemini:
  api-key: ${GEMINI_API_KEY:}

server:
  port: 8080
spring:
  application:
    name: productservice
  datasource:
    url: jdbc:postgresql://aws-0-eu-west-1.pooler.supabase.com:6543/postgres?prepareThreshold=0
    username: postgres.uwqjtdwytjcnbjhndsfl
    password: ADA-postgres
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: false
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
